"use_client";

import "../app/globals.css";

import Navbar from "@/components/Navbar";
import { useRouter } from "next/router";
import { ReactElement } from "react";

const Featured = () => {
  return (
    <div className="">

      TODO: this page should fetch currently featured steam games and initialize a similar list as top100, but with those games instead

    </div>
  );
};

export default Featured;
